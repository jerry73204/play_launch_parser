cmake_minimum_required(VERSION 3.8)
project(play_launch_parser)

# This is a Rust package using colcon-cargo-ros2
# The build is handled automatically by colcon-cargo-ros2

# Find dependencies
find_package(ament_cmake REQUIRED)

ament_package()
